# Production environment — copy to .env.production on the VM (gitignored).
# Loaded by docker-compose.prod.yml. The retriever needs no API keys:
# no ANTHROPIC_API_KEY here, enrichment never runs in prod.

RETRIEVER_HOST=0.0.0.0
RETRIEVER_PORT=8001
VECTORDB_DIR=/app/data/vectordb
